<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Register</title>

  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            brand: { 600: '#5457e0', 700: '#4f46e5' }
          },
          boxShadow: { soft: '0 10px 25px -10px rgba(0,0,0,.15)' }
        }
      }
    }
  </script>
</head>
<body class="min-h-screen bg-gradient-to-b from-brand-50 to-white flex items-center justify-center p-4">
  <main class="w-full max-w-md rounded-2xl bg-white shadow-soft border border-slate-200 p-8">
    <h1 class="text-2xl font-bold text-center mb-6">Create an Account</h1>

    <form action="/register" method="post" class="space-y-4">
      <input type="text" name="username" placeholder="Username" required
             class="w-full rounded-xl border border-slate-300 px-3 py-2 focus:ring-2 focus:ring-brand-600 outline-none"/>
      <input type="text" name="name" placeholder="Full Name" required
             class="w-full rounded-xl border border-slate-300 px-3 py-2 focus:ring-2 focus:ring-brand-600 outline-none"/>
      <input type="number" name="age" placeholder="Age" required
             class="w-full rounded-xl border border-slate-300 px-3 py-2 focus:ring-2 focus:ring-brand-600 outline-none"/>
      <input type="email" name="email" placeholder="Email" required
             class="w-full rounded-xl border border-slate-300 px-3 py-2 focus:ring-2 focus:ring-brand-600 outline-none"/>
      <input type="password" name="password" placeholder="Password" required
             class="w-full rounded-xl border border-slate-300 px-3 py-2 focus:ring-2 focus:ring-brand-600 outline-none"/>

      <input type="submit" value="Register"
             class="w-full cursor-pointer rounded-xl bg-brand-600 text-white font-semibold py-2 hover:bg-brand-700 transition"/>
    </form>

    <p class="mt-4 text-center text-sm text-slate-600">
      Already have an account?
      <a href="/login" class="text-brand-700 font-medium hover:underline">Login</a>
    </p>
  </main>
</body>
</html>
